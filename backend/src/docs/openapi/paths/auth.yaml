register:
  post:
    tags:
      - Authentication
    summary: Register a new user
    operationId: register
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../components/schemas.yaml#/RegisterRequest'
    responses:
      '201':
        description: User created; access and refresh tokens set as HTTP-only cookies
        content:
          application/json:
            schema:
              $ref: '../components/schemas.yaml#/RegisterResponse'
      '400':
        $ref: '../components/responses.yaml#/ValidationError'
      '409':
        $ref: '../components/responses.yaml#/Conflict'
      '429':
        $ref: '../components/responses.yaml#/RateLimited'
      '500':
        $ref: '../components/responses.yaml#/InternalError'

login:
  post:
    tags:
      - Authentication
    summary: Login with email and password
    operationId: login
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../components/schemas.yaml#/LoginRequest'
    responses:
      '200':
        description: Login successful; access and refresh tokens set as HTTP-only cookies
        content:
          application/json:
            schema:
              $ref: '../components/schemas.yaml#/LoginResponse'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '429':
        $ref: '../components/responses.yaml#/RateLimited'
      '500':
        $ref: '../components/responses.yaml#/InternalError'

logout:
  post:
    tags:
      - Authentication
    summary: Logout and invalidate tokens
    operationId: logout
    security:
      - cookieAuth: []
    responses:
      '200':
        $ref: '../components/responses.yaml#/SuccessMessage'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '500':
        $ref: '../components/responses.yaml#/InternalError'

refresh:
  post:
    tags:
      - Authentication
    summary: Refresh access token using refresh token cookie
    operationId: refreshAccessToken
    security:
      - refreshCookieAuth: []
    responses:
      '200':
        $ref: '../components/responses.yaml#/SuccessMessage'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '403':
        description: Refresh token has been revoked
        content:
          application/json:
            schema:
              $ref: '../components/schemas.yaml#/Error'
      '429':
        $ref: '../components/responses.yaml#/RateLimited'
      '500':
        $ref: '../components/responses.yaml#/InternalError'

me:
  delete:
    tags:
      - Authentication
    summary: Delete own account
    operationId: deleteAccount
    security:
      - cookieAuth: []
    responses:
      '200':
        $ref: '../components/responses.yaml#/SuccessMessage'
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '500':
        $ref: '../components/responses.yaml#/InternalError'

sessions:
  get:
    tags:
      - Authentication
    summary: List active sessions
    operationId: listSessions
    security:
      - cookieAuth: []
    responses:
      '200':
        description: Active sessions with device metadata
        content:
          application/json:
            schema:
              type: object
              properties:
                data:
                  type: array
                  items:
                    $ref: '../components/schemas.yaml#/Session'
                total:
                  type: integer
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '500':
        $ref: '../components/responses.yaml#/InternalError'

sessionsById:
  delete:
    tags:
      - Authentication
    summary: Revoke a single session
    operationId: revokeSession
    security:
      - cookieAuth: []
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
    responses:
      '200':
        description: Session revoked
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
      '401':
        $ref: '../components/responses.yaml#/Unauthorized'
      '404':
        $ref: '../components/responses.yaml#/NotFound'
      '500':
        $ref: '../components/responses.yaml#/InternalError'
